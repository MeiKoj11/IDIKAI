{
  "name": "idikai-server",
  "version": "1.0.0",
  "description": "Backend for Idikai: Claude API calls (dictionary lookup, grammar checks, etc.) plus accounts and per-user data sync via Node's built-in SQLite module. No npm dependencies — Node built-ins only.",
  "main": "server.js",
  "engines": {
    "node": ">=22.12.0 <23.0.0"
  },
  "scripts": {
    "start": "node server.js"
  }
}
